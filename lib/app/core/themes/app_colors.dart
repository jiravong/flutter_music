import 'package:flutter/material.dart';

class AppColors {
  // Primary Pink from Logo
  static const Color primary = Color(0xFFF0849B);
  static const Color primaryLight = Color(0xFFF7B5C6);
  static const Color primaryDark = Color(0xFFD66A81);

  // Background
  static const Color background = Color(0xFFFEF9FA);
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF2D2D2D);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textLight = Colors.white;

  // Status
  static const Color success = Color(0xFF4CAF50); // สำหรับสถานะ Done (status = 1)
  static const Color error = Color(0xFFE57373);   // สำหรับสถานะ Fail (status = 2)
}